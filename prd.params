# --- Base settings, shared by all environments --------------------------
#
# SOURCE_DIR = folder in this repo created by CPI's Git Push (exploded iFlow
#              project: META-INF/, src/main/resources/, .project, metainfo.prop)
# BASE_ID    = base artifact ID; the pipeline appends _QUA / _PRD to it
#
# DEV package (your manual workspace, pipeline never touches it):
#   GitHubIntegrationwithSAPIntegrationSuiteDEV

SOURCE_DIR=Test_iFlow_DEV
BASE_ID=Test_iFlow
IFLOW_NAME="Test iFlow"
